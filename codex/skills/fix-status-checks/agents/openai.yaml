interface:
  display_name: "Fix status checks"
  short_description: "Inspect, fix, and push"
  default_prompt: "Fix the failing status checks on this PR: inspect what failed, fix the issue, and push."
