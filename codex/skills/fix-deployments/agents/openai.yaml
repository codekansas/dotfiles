interface:
  display_name: "Fix Deployments"
  short_description: "Repair deployments in safe order"
  default_prompt: "Use $fix-deployments to repair deployment environments in risk order, merging follow-up fixes until the default branch checks and environments are healthy."
