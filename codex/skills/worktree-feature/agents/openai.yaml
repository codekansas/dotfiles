interface:
  display_name: "Worktree Feature"
  short_description: "Build features in isolated worktrees"
  default_prompt: "Use $worktree-feature to create a repo-task worktree, implement the feature there, open a PR, merge or queue it, then remove the worktree without waiting for production."
