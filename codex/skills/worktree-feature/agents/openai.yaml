interface:
  display_name: "Worktree Feature"
  short_description: "Build features in isolated worktrees"
  default_prompt: "Use $worktree-feature to create a new repo-task worktree, implement the requested feature there, open and merge a PR, verify the base branch, pull the original checkout, then remove the feature worktree."
