interface:
  display_name: "Optimize"
  short_description: "Run an autonomous optimization loop with helper subagents"
  default_prompt: "Use $optimize with a natural-language optimization goal, stay alive as the long-running coordinator, run the experiments yourself, delegate each bounded results-and-next-step update to $do-optimize-step, and call $fix-optimize-loop whenever the optimization loop hits a bug."
