interface:
  display_name: "Optimize"
  short_description: "Run infinite optimization with metric logs"
  default_prompt: "Use $optimize with a natural-language optimization goal, infer a consistent metric protocol from the codebase, and run an unbounded optimization loop until stopped."
